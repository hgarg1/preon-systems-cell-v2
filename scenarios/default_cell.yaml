# Legacy filename retained only so older local paths fail softly.
# Scenario YAML is no longer loaded by the application.
organism:
  identity_profile:
    name: Preon Organism
    purpose: Deterministic organism runtime
  goals:
    - Process admitted signals
    - Emit validated proteins
  default_signal:
    type: calculate
    payload:
      expression: "21 * 2"
